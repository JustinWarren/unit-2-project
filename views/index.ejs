<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title></title>
</head>
<body>
  <h1>Trips Index Page</h1>
    <ul>
      <% for(let i = 0; i < trips.length; i++) { %>
        <a href="/trips/<%=trips[i].id; %>">
        <li><%=trips[i].nationalpark; %></li></a>
        <li><%=trips[i].state; %></li>
        <li><%=trips[i].season; %></li>
        <li><%=trips[i].memory; %></li>
        <li><%=trips[i].img; %></li>
      <% } %>
    </ul>

    <form class="" action="/trips/new">
      <input type="submit" name="" value="Create New Trip">
    </form>
  </body>
  </html>
